services:
  app:
    build: .
    container_name: titanic-http-server
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: npm run dev -- --input ./titanic.json --host 0.0.0.0 --port 3000

volumes:
  node_modules:
